CREATE TABLE muscles (
    id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name TEXT NOT NULL UNIQUE
);

ALTER TABLE exercises
ADD COLUMN main_muscle_worked_id INT NOT NULL REFERENCES muscles (id);
